{
  "name": "corpo-lingo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @corpo-lingo/shared build && pnpm run --parallel dev",
    "nx-dev": "nx run-many -t dev",
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "nx": "22.6.5",
    "typescript": "^6.0.3"
  }
}